no repositorio final vou remover esse arquivo ok

falta:
fazer programa fechar quando a tela de adm for fechada

adicionar socket.py e communicator.py (evitar serial.py pra nao conflitar com a biblioteca)
socket só cria o websocket, e communicator pega os dados do serial e publica todos nele, sem nenhuma formatação (formatação será responsabilidade do front)